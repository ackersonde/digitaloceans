machine:
  services:
    - docker

dependencies:
  override:
    - go get -t -d -v ./...
    - ./set_env.sh
    - cat digitalocean_ignition.json

deployment:
  live:
    branch: master
    commands:
      - go run do.go

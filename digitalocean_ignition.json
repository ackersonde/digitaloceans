{
   "ignition": { "version": "2.0.0" },
   "systemd": {
     "units": [
       {
         "name": "docker.service",
         "enable": true
       },
       {
         "name": "cleanbbgames.service",
         "contents": "[Unit]\nDescription=Removes old ball games from /home/{{deploy_user}}/bb_games/\n\n[Service]\nType=oneshot\nExecStart=find /home/{{deploy_user}}/bb_games/* -mtime +5 -exec rm {} \;"
       },
       {
         "name": "cleanbbgames.timer",
         "enable": true,
         "contents": "[Unit]\nDescription=Run cleanbbgames.service every day\n\n[Timer]\nOnCalendar=daily\n\n[Install]\nWantedBy=multi-user.target"
       }
     ]
   },
   "passwd": {
    "users": [{
      "name": "{{deploy_user}}",
      "passwordHash": "{{console_passwd_hash}}",
      "sshAuthorizedKeys": [ "{{circleci_deploy_pubkey}}", "{{login_ssh_pubkey}}" ],
      "create": {
        "groups": ["sudo", "docker"]
      }
    }]
  },
  "storage": {
    "files": [{
        "filesystem": "root",
        "path": "/etc/ssh/sshd_config",
        "contents": { "source": "data:text/plain;base64,{{encoded_sshd_config}}" }
    }]
  }
}
